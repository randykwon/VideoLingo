services:
  worker:
    build:
      context: .
      dockerfile: Dockerfile.cuda
    environment:
      WHISPER_DEVICE: cuda
      WHISPER_COMPUTE_TYPE: float16
    gpus: all
  ollama:
    gpus: all
